# `@leokok/poke-agents` (legacy compatibility shim)

This package is kept for backward compatibility.

If you run:

```bash
npx @leokok/poke-agents
```

it now forwards to:

```bash
npx poke-agents@latest
```

Please migrate scripts and docs to the canonical package:

```bash
npx poke-agents@latest
```
